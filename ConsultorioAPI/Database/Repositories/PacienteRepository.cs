using ConsultorioAPI.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace ConsultorioAPI.Database.Repositories
{
    //public class PacienteRepository
    //{
    //    public Paciente GetPacienteFromUsername(string username)
    //    {
    //        using (var ctx = new ConsultorioDbContext())
    //        {
    //            LoginUsuario loginUsuario = ctx.Usuarios.FirstOrDefault(x => x.UserName == username);
    //            if (loginUsuario == null)
    //                return null;

    //            return ctx.Pacientes.FirstOrDefault(p => p.DadosLogin.Id.Equals(loginUsuario.Id));
    //        }
    //    }
    //}
}